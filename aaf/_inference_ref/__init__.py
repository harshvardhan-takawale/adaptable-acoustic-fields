"""Vendored INFER reference code — DO NOT IMPORT IN PRODUCTION CODE.

The two modules here (`inference_model`, `inference_renderer`) are minimal
extracts from the INFER (ICML'26 submission) codebase, kept verbatim for
side-by-side diffing during the 2D port in Chunks 2-3.

Production code lives in `aaf.models`, `aaf.renderers`, etc. Don't import
from this package.

Provenance: project_files/unified_models.py and project_files/unified_renderers.py
in the multimodal_recon working directory.
"""
