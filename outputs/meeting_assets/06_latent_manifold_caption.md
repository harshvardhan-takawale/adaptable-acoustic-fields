**Latent manifold of C1 FiLM (PC1 vs L).** The trained latents form an almost-monotonic 1-D manifold parameterised by L; the PC1-vs-L linear fit reaches R² = 0.987 (target was > 0.7, never met by the 9 R-runs). The model HAS learned that 'room length' is the right axis. The remaining gap is decoding: even when handed a good latent for an unseen L, the spectrum rendering doesn't follow.
