run_id: M2_45rooms_d32
rooms_yaml: configs/sweeps_3d/train_rooms.yaml

# D29: d=32 hedge insurance against d=16 under-fitting the 3D manifold.
# All other params identical to M1_45rooms.yaml.
latent_dim: 32
log2_hashmap_size: 18
n_levels: 16
per_level_scale: 1.38
conditioning_type: film
latent_jitter_sigma: 0.1
l_head_enabled: true
l_head_weight: 0.1

n_iters: 30000
batch_size: 4
grad_accum_steps: 1
lr_network: 2e-4
lr_latent: 1e-3
lambda_latent: 1e-4

n_azi: 16
n_ele: 16
n_pts_per_ray: 16

val_every: 1000
ckpt_every: 2500
