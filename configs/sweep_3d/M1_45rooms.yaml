run_id: M1_45rooms
rooms_yaml: configs/sweeps_3d/train_rooms.yaml

# Architecture (D19-D23)
latent_dim: 16
log2_hashmap_size: 18
n_levels: 16
per_level_scale: 1.38
conditioning_type: film
latent_jitter_sigma: 0.1
l_head_enabled: true
l_head_weight: 0.1

# Optimization (D24-D26)
n_iters: 30000
batch_size: 4
grad_accum_steps: 1
lr_network: 2e-4
lr_latent: 1e-3
lambda_latent: 1e-4

# Renderer (D8 + D12 cascade)
n_azi: 16
n_ele: 16
n_pts_per_ray: 16

# Cadences
val_every: 1000
ckpt_every: 2500
